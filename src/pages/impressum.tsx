import React from 'react'
import { layoutStyles } from './unsere-tiere'

export default () => (
  <main className={layoutStyles.mainContainer}>
    <h1 className={layoutStyles.mainHeadline}>Impressum</h1>
  </main>
)
